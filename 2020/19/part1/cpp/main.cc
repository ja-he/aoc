#include <iomanip>
#include <iostream>

#include <algorithm>
#include <memory>
#include <numeric>

#include <bitset>
#include <map>
#include <optional>
#include <string>
#include <variant>
#include <vector>

#include <cassert>
#include <cctype>

int
main(void)
{

  // get linewise input
  std::vector<std::string> input;
  {
    std::string line;
    while (std::getline(std::cin, line)) { input.push_back(line); }
  }

}
